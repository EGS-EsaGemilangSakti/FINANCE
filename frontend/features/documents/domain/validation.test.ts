import { describe,expect,it } from "vitest";
import { DOCUMENT_MAX_SIZE_BYTES,safeFileName,validateUploadMetadata } from "./validation";
describe("document metadata validation",()=>{it.each([["C:\\fakepath\\contract.pdf","contract.pdf"],["/tmp/customer/report.png","report.png"],["simple.jpg","simple.jpg"]])("normalizes basename %s",(input,expected)=>expect(safeFileName(input)).toBe(expected));it("rejects empty, controls and long names",()=>{expect(safeFileName("")).toBeUndefined();expect(safeFileName("bad\u0000.pdf")).toBeUndefined();expect(safeFileName(`${"a".repeat(181)}.pdf`)).toBeUndefined();});it("uses MIME and size, not extension alone",()=>{expect(validateUploadMetadata({originalFileName:"fake.pdf",displayFileName:"fake.pdf",mimeType:"text/html",sizeBytes:100}).errors.mimeType).toBeDefined();expect(validateUploadMetadata({originalFileName:"photo.exe",displayFileName:"photo.exe",mimeType:"image/png",sizeBytes:100}).valid).toBe(false);expect(validateUploadMetadata({originalFileName:"photo.png",displayFileName:"photo.png",mimeType:"image/png",sizeBytes:100}).valid).toBe(true);expect(validateUploadMetadata({originalFileName:"zero.pdf",displayFileName:"zero.pdf",mimeType:"application/pdf",sizeBytes:0}).valid).toBe(false);expect(validateUploadMetadata({originalFileName:"large.pdf",displayFileName:"large.pdf",mimeType:"application/pdf",sizeBytes:DOCUMENT_MAX_SIZE_BYTES+1}).valid).toBe(false);});});
