"use client";import { useSearchParams } from "next/navigation";import type { CommandActor } from "@/features/projects/domain/commands";
export type RateAccess="manage"|"editor"|"view"|"none";
export function resolveRateAccess(value:string|null):RateAccess{return value==="editor"||value==="view"||value==="none"?value:"manage";}
export function rateUrl(path:string,access:RateAccess,extra?:Readonly<Record<string,string|undefined>>){const params=new URLSearchParams();for(const [key,value] of Object.entries(extra??{}))if(value)params.set(key,value);params.set("access",access);return`${path}?${params}`;}
export function useRateAccess(){const access=resolveRateAccess(useSearchParams().get("access"));const permissionKeys=access==="manage"?["rate.view","rate.manage","rate.activate","audit.view"]:access==="editor"?["rate.view","rate.manage","audit.view"]:access==="view"?["rate.view","audit.view"]:[];const actor:CommandActor={userId:access==="manage"?"usr-raka":"usr-dewi",permissionKeys};return{access,actor,canView:permissionKeys.includes("rate.view"),canManage:permissionKeys.includes("rate.manage"),canActivate:permissionKeys.includes("rate.activate"),canAudit:permissionKeys.includes("audit.view"),url:(path:string,extra?:Readonly<Record<string,string|undefined>>)=>rateUrl(path,access,extra)};}
