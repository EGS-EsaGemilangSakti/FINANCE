import type { AccessListQuery,NormalizedAccessListQuery,PermissionDefinition,PermissionKey,SodFinding } from "./types";
export const permissionCatalog:readonly PermissionDefinition[]=[{key:"access.view",module:"Access",label:"Lihat administrasi akses",description:"Melihat user, role, dan audit akses.",risk:"Sensitive"},{key:"access.manage",module:"Access",label:"Kelola user dan role",description:"Mengubah user, role, dan permission matrix.",risk:"Critical"},{key:"project.view",module:"Project",label:"Lihat project",description:"Melihat project sesuai scope.",risk:"Standard"},{key:"project.manage",module:"Project",label:"Kelola project",description:"Membuat dan memperbarui project.",risk:"Sensitive"},{key:"pic.approve",module:"Approval",label:"Final approval PIC",description:"Memberikan keputusan final sebagai PIC.",risk:"Critical"}];
const unique=<T extends string>(values:readonly T[]|undefined)=>[...new Set(values??[])].sort();
export function normalizeAccessListQuery(input:AccessListQuery={}):NormalizedAccessListQuery{return{search:(input.search??"").trim().toLocaleLowerCase("id-ID"),statuses:unique(input.statuses),roleIds:unique(input.roleIds),sort:input.sort??"updatedAt",direction:input.direction??"desc",page:Math.max(1,Math.trunc(input.page??1)),pageSize:[10,20,50].includes(input.pageSize??20)?input.pageSize??20:20};}
export function evaluateSod(keys:readonly PermissionKey[]):readonly SodFinding[]{return keys.includes("access.manage")&&keys.includes("pic.approve")?[{ruleId:"SOD-ADMIN-FINAL-APPROVAL",severity:"Critical",permissionKeys:["access.manage","pic.approve"],explanation:"Pengelola user/role tidak boleh menjadi final approver transaksi.",guidance:"Hapus permission final approval PIC atau permission administrasi akses.",blocking:true}]:[];}
export function resolveEffectivePermissions(roleIds:readonly string[],roles:readonly {id:string;status:string;permissionKeys:readonly PermissionKey[]}[]){return unique(roles.filter(role=>role.status==="Active"&&roleIds.includes(role.id)).flatMap(role=>role.permissionKeys));}
export function permissionGroups(){return Object.groupBy(permissionCatalog,item=>item.module);}
