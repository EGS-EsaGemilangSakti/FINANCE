"use client";
import {useRef,useState} from "react";
import {Button,Drawer} from "@/components/ui";
import {formatDate,formatRupiah} from "@/lib/formatters";
import type {PayrollBillingLine} from "../domain/types";
export function PayrollCalculationTrace({lines,currency}:{lines:readonly PayrollBillingLine[];currency:string}){const [lineId,setLineId]=useState<string>(),trigger=useRef<HTMLButtonElement|null>(null),line=lines.find(item=>item.id===lineId),close=()=>{setLineId(undefined);requestAnimationFrame(()=>trigger.current?.focus());};return <>{lines.map(item=><Button key={item.id} variant="quiet" onClick={event=>{trigger.current=event.currentTarget;setLineId(item.id);}}>Jelaskan {item.employeeReference}</Button>)}<Drawer open={Boolean(line)} title={line?`Calculation trace ${line.employeeReference}`:"Calculation trace"} description="Read-only metadata dari revision calculation" onClose={close}>{line&&<div className="trace-grid"><dl className="detail-list"><dt>Source</dt><dd>{line.employeeReference}</dd><dt>Branch/service</dt><dd>{line.branchLabel} · {line.serviceLabel}</dd><dt>Attendance</dt><dd>{line.trace.attendanceBatchId} · R{line.trace.attendanceRevision} · {line.trace.sourceRowId}</dd><dt>Quantity/unit</dt><dd>{line.trace.quantity} {line.trace.unit}</dd><dt>Rate Version/line</dt><dd>{line.trace.rateVersionId} · {line.trace.rateLineId}</dd><dt>Rate snapshot</dt><dd>{formatRupiah(line.trace.unitRate)}</dd><dt>Formula metadata</dt><dd>{line.trace.formulaLabel}</dd><dt>Base subtotal</dt><dd>{formatRupiah(line.componentSubtotal)}</dd><dt>Adjustment</dt><dd>{formatRupiah(line.adjustment)}</dd><dt>Tax basis</dt><dd>Tidak berlaku</dd><dt>Withholding</dt><dd>{line.withholding==="0"?"Tidak berlaku":formatRupiah(line.withholding)}</dd><dt>Rounding</dt><dd>{line.trace.roundingMethod}</dd><dt>Final amount</dt><dd>{formatRupiah(line.finalAmount)} · {currency}</dd><dt>Calculated</dt><dd>{formatDate(line.trace.calculatedAt,true)}</dd></dl><h3>Component sequence</h3><ol className="audit-list">{line.components.map(component=><li key={component.code}><strong>{component.code} · {component.label}</strong><span>{component.applicability==="NotApplicable"?"Tidak berlaku":formatRupiah(component.amount)}</span><small>{component.source}</small></li>)}</ol></div>}</Drawer></>}
