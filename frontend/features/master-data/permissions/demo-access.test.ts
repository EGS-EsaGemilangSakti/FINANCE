import { describe,expect,it } from "vitest";import { masterUrl,resolveMasterAccess } from "./demo-access";describe("master persona navigation",()=>{it("preserves persona",()=>expect(masterUrl("/master-data/customers/cus-aruna/edit","view",{scenario:"conflict"})).toBe("/master-data/customers/cus-aruna/edit?scenario=conflict&access=view"));it("resolves denied persona",()=>expect(resolveMasterAccess("none")).toBe("none"));});
