import { describe,expect,it } from "vitest";import { normalizeMasterListQuery } from "./list-query";describe("normalizeMasterListQuery",()=>{it("normalizes search, arrays and pagination",()=>expect(normalizeMasterListQuery({search:"  ARUNA ",statuses:["Inactive","Active","Active"],page:-1,pageSize:500})).toEqual({search:"aruna",statuses:["Active","Inactive"],sort:"updatedAt",direction:"desc",page:1,pageSize:100}));});
