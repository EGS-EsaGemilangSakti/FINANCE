import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { PartyDetailPage } from "@/features/master-data/components/party-detail-page";export default async function Page(props:PageProps<"/master-data/customers/[customerId]">){const {customerId}=await props.params;return <Suspense fallback={<LoadingState/>}><PartyDetailPage kind="customers" id={customerId}/></Suspense>}
