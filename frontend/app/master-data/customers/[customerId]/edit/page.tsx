import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { MasterFormPage } from "@/features/master-data/components/master-form-page";export default async function Page(props:PageProps<"/master-data/customers/[customerId]/edit">){const {customerId}=await props.params;return <Suspense fallback={<LoadingState/>}><MasterFormPage kind="customers" id={customerId}/></Suspense>}
