import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { MasterFormPage } from "@/features/master-data/components/master-form-page";export default async function Page(props:PageProps<"/master-data/vendors/[vendorId]/edit">){const {vendorId}=await props.params;return <Suspense fallback={<LoadingState/>}><MasterFormPage kind="vendors" id={vendorId}/></Suspense>}
