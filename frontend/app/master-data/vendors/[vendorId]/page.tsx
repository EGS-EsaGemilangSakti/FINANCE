import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { PartyDetailPage } from "@/features/master-data/components/party-detail-page";export default async function Page(props:PageProps<"/master-data/vendors/[vendorId]">){const {vendorId}=await props.params;return <Suspense fallback={<LoadingState/>}><PartyDetailPage kind="vendors" id={vendorId}/></Suspense>}
