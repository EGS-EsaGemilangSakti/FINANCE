import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { MasterFormPage } from "@/features/master-data/components/master-form-page";export default async function Page(props:PageProps<"/master-data/services/[serviceId]/edit">){const {serviceId}=await props.params;return <Suspense fallback={<LoadingState/>}><MasterFormPage kind="services" id={serviceId}/></Suspense>}
