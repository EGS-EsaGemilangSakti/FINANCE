import {InvoiceListPage} from "@/features/invoices/components/invoice-list-page";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams;const access=typeof query.access==="string"?query.access:"manage";const search=typeof query.search==="string"?query.search:"";const page=Math.max(1,Number(typeof query.page==="string"?query.page:"1")||1);const raw=Number(typeof query.pageSize==="string"?query.pageSize:"20");const pageSize=raw===10||raw===50?raw:20;return <InvoiceListPage access={access} initialSearch={search} initialPage={page} initialPageSize={pageSize}/>;}
