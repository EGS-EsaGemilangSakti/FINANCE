import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { AdminFormPage } from "@/features/access-management/components/admin-form-page";export default async function Page(props:{params:Promise<{userId:string}>}){const{userId}=await props.params;return <Suspense fallback={<LoadingState/>}><AdminFormPage kind="users" mode="edit" id={userId}/></Suspense>}
