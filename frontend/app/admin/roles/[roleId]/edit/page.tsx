import { Suspense } from "react";import { LoadingState } from "@/components/ui";import { AdminFormPage } from "@/features/access-management/components/admin-form-page";export default async function Page(props:{params:Promise<{roleId:string}>}){const{roleId}=await props.params;return <Suspense fallback={<LoadingState/>}><AdminFormPage kind="roles" mode="edit" id={roleId}/></Suspense>}
